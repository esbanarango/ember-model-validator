  <p>Configuration options:</p>
  <ul>
    <li>
      <p>
        <code class="ui label">:message</code> - A custom error message (default is: “{{defaultMessage}}”). it can be a String or a <a href="https://github.com/esbanarango/ember-model-validator#using-function-to-generate-custom-message">function</a>  that returns a string.
      </p>
    </li>
    <li>
      <p>
        <code class="ui label">:allowBlank</code> - If set to true and the value is blank as defined by Ember.isBlank, all other validations for the field are skipped.
      </p>
    </li>
  </ul>